# Audio: nunca al repo (9+ GB)
*.mp3
*.wav
*.flac
nebulosa/
ensayos/

.venv/
__pycache__/
*.pyc
.DS_Store

# Caché de features y salidas
*.db
.zapaia_cache.db
ranking*.csv
ventanas*.csv
scores.csv
log.txt

# playlists con rutas absolutas locales
*.m3u

# CLAUDE.md sí va en este repo (el gitignore global lo excluye por defecto)
!CLAUDE.md

# clips y lotes de comparación A/B
feedback/

*.png
*.mp4
*_cuadros/

# IDs de Drive y claves: NUNCA al repo (es público)
zapaia_local.json
# salidas de compilados
compilado_*.txt

# fotos de la banda (bajadas de Drive para los videos): NUNCA al repo
imagenes/
compilado_*.json

# credenciales de Google: NUNCA acá
client_secret*.json
*.apps.googleusercontent.com.json

# ground truth con nombres de tomas y juicios: NUNCA al repo público
refs.txt
refs.local.txt
